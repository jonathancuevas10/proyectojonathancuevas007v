import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'proyectojonathancuevas007v',
  webDir: 'www'
};

export default config;
